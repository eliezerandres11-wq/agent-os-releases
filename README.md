# Agent OS — downloads

This repo hosts install downloads and the update-check file for Agent OS.
The source code lives in a private repository; this one exists only so a
recipient's Mac can fetch a disk image and check for updates without needing
a GitHub account.

- Latest disk image: see [Releases](../../releases/latest)
- Update-check file: `latest.json`
